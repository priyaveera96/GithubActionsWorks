name: Boolean Test
on: 
  workflow_dispatch:

 
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test: [name1, name2, name3]
    steps:
       - uses: actions/checkout@v3
       - name: android print statement       
         run: | 
           if [ "${{matrix.test}}" == "name3" ]; then
             echo "lambda=true" >> $GITHUB_ENV
             echo "testname: ${{matrix.test}} + lambda : $lambda"
           fi  
           echo "testname: ${{matrix.test}} + lambda : $lambda"



  
