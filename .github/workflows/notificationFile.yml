name: Send Email with HTML Body

on: workflow_dispatch

env:
 html_content: ""

jobs:
  sendemail:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Read HTML file content
        id: read-html
        run: |
          html_content=$(cat test.html)
          cat test.html   
     
      - name: Send mail notification
        if: ${{ always() }}
        run: |
          curl -s --url "smtps://smtp.gmail.com:587" \
               --ssl-reqd \
               --mail-from "notificsunpower@gmail.com" \
               --mail-rcpt "priyaveera96@gmail.com" \
               --user "notificsunpower@gmail.com:xhbexzslqdxniljz" \
               --insecure \
               -T test.html \
               --upload-file test.html \
               --header "Content-Type: text/html"

